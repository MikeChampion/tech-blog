
<a href="/dashboard/new" class="btn border-2 text-white bg-blue-900 px-2 py-1 rounded-lg mt-4">+ New Post</a>

{{#if posts.length}}
<div class="flex flex-row border-4 rounded-2xl bg-gray-600">
<h2 class="font-bold">Your Posts</h2>
    {{#each posts as |post|}}
      {{> dashboard-postinfo post}}
    {{/each}}

</div>
{{/if}}

<script src="/js/add-post.js"></script>