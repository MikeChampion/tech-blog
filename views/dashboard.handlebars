
<a href="/dashboard/new" class="btn border-2 text-white bg-blue-900 px-2 py-1 rounded-lg mt-2">+ New Post</a>

{{#if posts.length}}
<div class="flex flex-col md:flex-row justify-center items-center border-4 rounded-2xl bg-gray-500 w-full">
<h2 class="font-bold">Your Posts</h2>
    {{#each posts as |post|}}
      {{> dashboard-postinfo post}}
    {{/each}}

</div>
{{/if}}

<script src="/js/add-post.js"></script>